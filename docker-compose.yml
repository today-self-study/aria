services:
  aria:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aria-spa
    ports:
      - "8200:80"
    restart: unless-stopped 
    networks:
      - sillasol-net

networks:
  sillasol-net:
    external: true